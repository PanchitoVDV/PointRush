# Stap 1: gebruik DIT eerst (zonder SSL-regels)
# sudo cp nginx-http-only.conf /etc/nginx/sites-available/pointrush.coresmp.nl
# sudo nginx -t && sudo systemctl reload nginx
# sudo certbot certonly --webroot -w /var/www/html -d pointrush.coresmp.nl
# Daarna stap 2: nginx.conf met SSL

server {
	listen 80;
	server_name pointrush.coresmp.nl;

	location / {
		proxy_pass http://127.0.0.1:3847;
		proxy_http_version 1.1;
		proxy_set_header Host $host;
		proxy_set_header X-Real-IP $remote_addr;
		proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
		proxy_set_header X-Forwarded-Proto $scheme;
	}
}
